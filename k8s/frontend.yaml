apiVersion: apps/v1
kind: Deployment
spec:
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: krmp-d2hub-idock.9rum.cc/dev-test/repo_c41a8b19357a
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
